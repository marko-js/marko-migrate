<if(data.items)>
    <for(item in data.items)>
        <div>${item.name}</div>
    </for>
</if>
<else>content</else>

~~~~~~~
if(data.items)
    for(item in data.items)
        div - ${item.name}
else - content
